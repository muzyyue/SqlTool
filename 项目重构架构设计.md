# SQL工具项目重构架构设计

## 🎯 重构目标

基于现有代码基础，实现一个功能完整、性能优异、用户体验良好的SQL生成工具，支持从Excel文件自动生成INSERT和UPDATE语句。

## 📋 功能需求分析

### 1. 核心功能模块
- **DDL解析器**：准确解析CREATE TABLE语句，提取表名、字段名、数据类型
- **Excel解析器**：支持.xlsx/.xls/.csv格式，处理大型文件（1000+行），多工作表支持
- **SQL生成器**：生成符合ANSI SQL标准的INSERT/UPDATE语句，支持主流数据库语法
- **智能字段绑定**：基于名称相似度的自动匹配 + 手动调整界面

### 2. 增强功能模块
- **动态字段管理**：支持自定义字段添加、数据类型设置、默认值配置
- **批量编辑功能**：多字段/记录统一编辑，批量替换、格式转换
- **精确字段过滤**：基于DDL字段定义进行精确过滤

### 3. UI/UX优化要求
- **响应式设计**：桌面端(1024px+)、平板端(768-1023px)、移动端(≤767px)
- **现代界面**：美观简洁，操作流程直观，引导提示清晰
- **性能优化**：大型文件进度显示，后台处理机制

## 🏗️ 技术架构设计

### 1. 分层架构
```
┌─────────────────────────────────────────┐
│             视图层 (Views)              │
│  - Home.vue (主界面)                    │
│  - Insert.vue (INSERT生成)              │
│  - Update.vue (UPDATE生成)              │
│  - Layout.vue (布局组件)                │
└─────────────────────────────────────────┘
┌─────────────────────────────────────────┐
│          业务逻辑层 (Composables)       │
│  - useDdlParser.js (DDL解析)           │
│  - useExcelParser.js (Excel解析)       │
│  - useSqlGenerator.js (SQL生成)        │
│  - useFieldMatcher.js (字段匹配)       │
│  - useBatchEditor.js (批量编辑)        │
│  - useErrorHandler.js (错误处理)       │
└─────────────────────────────────────────┘
┌─────────────────────────────────────────┐
│            数据层 (Data Layer)          │
│  - Excel数据模型                       │
│  - DDL解析结果                         │
│  - 字段映射关系                        │
│  - 用户配置数据                        │
└─────────────────────────────────────────┘
```

### 2. 核心模块设计

#### 2.1 DDL解析器 (useDdlParser.js)
```javascript
// 功能：支持多种数据库DDL语法解析
// 特性：
// - 支持MySQL/PostgreSQL/SQL Server语法
// - 提取表名、字段名、数据类型、约束信息
// - 容错处理，支持不完整的DDL语句
// - 缓存机制，避免重复解析
```

#### 2.2 Excel解析器 (useExcelParser.js)
```javascript
// 功能：高性能Excel文件解析
// 特性：
// - 支持.xlsx/.xls/.csv格式
// - 多工作表支持
// - 流式处理大型文件（1000+行）
// - 数据类型自动识别
// - 进度回调机制
```

#### 2.3 智能字段匹配 (useFieldMatcher.js)
```javascript
// 功能：DDL字段与Excel列智能匹配
// 特性：
// - 基于名称相似度算法（Levenshtein距离）
// - 拼音首字母匹配
// - 手动调整界面
// - 映射关系可视化
```

## 🎨 UI/UX设计规范

### 1. 设计原则
- **简洁性**：界面元素精简，避免信息过载
- **一致性**：统一的视觉风格和交互模式
- **可访问性**：支持键盘操作，良好的对比度
- **响应式**：多端适配，流畅的交互体验

### 2. 页面布局
```
┌─────────────────────────────────────────┐
│  Header: 导航菜单 + 标题                │
├─────────────────────────────────────────┤
│  Main Content:                          │
│  ┌─────────────┬──────────────────────┐ │
│  │ 左侧配置区  │  右侧结果预览区       │ │
│  │ - 文件上传  │ - SQL语句预览        │ │
│  │ - DDL输入   │ - 语法高亮          │ │
│  │ - 字段映射  │ - 复制/导出功能     │ │
│  └─────────────┴──────────────────────┘ │
└─────────────────────────────────────────┘
```

### 3. 响应式断点
- **桌面端**：≥1024px - 左右分栏布局
- **平板端**：768-1023px - 上下分栏布局
- **移动端**：≤767px - 单列堆叠布局

## 🔧 性能优化策略

### 1. 文件处理优化
- **分块读取**：大型Excel文件分块处理
- **Web Worker**：后台线程处理计算密集型任务
- **虚拟滚动**：大数据量时的列表渲染优化

### 2. 内存管理
- **数据清理**：及时释放不再使用的数据
- **缓存策略**：合理使用缓存减少重复计算
- **懒加载**：按需加载模块和组件

### 3. 用户体验优化
- **进度指示**：文件上传和处理进度显示
- **错误恢复**：友好的错误提示和恢复机制
- **操作反馈**：即时反馈用户操作结果

## 🛡️ 错误处理与日志

### 1. 错误分类
- **输入验证错误**：文件格式、DDL语法等
- **处理错误**：解析失败、生成失败等
- **系统错误**：内存不足、网络问题等

### 2. 错误处理策略
- **预防性检查**：操作前的参数验证
- **优雅降级**：主要功能失败时的备用方案
- **用户友好提示**：清晰的错误信息和解决建议

### 3. 日志系统
- **操作日志**：记录用户关键操作
- **错误日志**：详细的错误信息和堆栈
- **性能日志**：关键操作的执行时间

## 📦 依赖管理

### 核心依赖
```json
{
  "vue": "^3.5.25",
  "ant-design-vue": "^4.2.6",
  "xlsx": "^0.18.5",
  "node-sql-parser": "^5.3.13",
  "pinyin-pro": "^3.27.0"
}
```

### 开发依赖
```json
{
  "vite": "^7.2.4",
  "eslint": "^9.39.1",
  "prettier": "3.6.2"
}
```

## 🚀 实施计划

### 第一阶段：基础架构重构（1-2天）
1. 清理项目结构，移除冗余代码
2. 建立模块化架构
3. 实现基础错误处理机制

### 第二阶段：核心功能重构（3-4天）
1. 重构DDL解析器
2. 优化Excel解析器
3. 实现智能字段匹配
4. 增强SQL生成器

### 第三阶段：UI/UX重构（2-3天）
1. 设计响应式界面
2. 优化用户交互流程
3. 实现性能优化功能

### 第四阶段：测试与优化（1-2天）
1. 功能测试
2. 性能测试
3. 用户体验测试
4. 代码优化和文档完善

## 📊 成功指标

### 功能指标
- ✅ 支持所有要求的文件格式
- ✅ 准确解析DDL语句
- ✅ 生成正确的SQL语句
- ✅ 实现智能字段匹配

### 性能指标
- ⚡ 1000行Excel文件处理时间 < 3秒
- ⚡ 页面加载时间 < 2秒
- ⚡ 内存使用稳定，无泄漏

### 用户体验指标
- 👍 操作流程直观，学习成本低
- 👍 错误提示清晰，恢复机制完善
- 👍 多端适配良好，交互流畅

---

此架构设计为项目重构提供了清晰的路线图和技术规范，确保最终交付的产品达到生产级质量标准。